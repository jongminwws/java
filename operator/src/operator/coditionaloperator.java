package operator;

public class coditionaloperator {
    public static void main(String[] args) {
        int score = 85;
//        char grade = score > 90 ? 65 : (score > 80 ? 66 : 67)
//        System.out.println("" + score + "점은" + grade + "등급");
    }
}